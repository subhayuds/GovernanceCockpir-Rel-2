_schema-version: "3.2"
ID: comhclbtpgovernancecockpit
description: Generated by Fiori Tools
version: 2.0.9
modules:
- name: comhclbtpgovernancecockpit-destination-content
  type: com.sap.application.content
  requires:
  - name: BTPGovernanceCockpit-Dest
    parameters:
      content-target: true
  - name: comhclbtpgovernancecockpit-repo-host
    parameters:
      service-key:
        name: comhclbtpgovernancecockpit-repo-host-key
  - name: comhclbtpgovernancecockpit-uaa
    parameters:
      service-key:
        name: comhclbtpgovernancecockpit-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comhclbtpgovernancecockpit_html_repo_host
          ServiceInstanceName: BTPGovernanceCockpit-HTML5
          ServiceKeyName: comhclbtpgovernancecockpit-repo-host-key
          sap.cloud.service: comhclbtpgovernancecockpit
        - Authentication: OAuth2UserTokenExchange
          Name: comhclbtpgovernancecockpit_uaa
          ServiceInstanceName: BTPGovernanceCockpit-XUAA
          ServiceKeyName: comhclbtpgovernancecockpit-uaa-key
          sap.cloud.service: comhclbtpgovernancecockpit
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comhclbtpgovernancecockpit-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comhclbtpgovernancecockpit-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comhclbtpgovernancecockpit.zip
      name: comhclbtpgovernancecockpit
      target-path: resources/
- name: comhclbtpgovernancecockpit
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: BTPGovernanceCockpit-Dest
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: BTPGovernanceCockpit-Dest
    service-plan: lite
- name: comhclbtpgovernancecockpit-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: BTPGovernanceCockpit-XUAA
    service-plan: application
- name: comhclbtpgovernancecockpit-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: BTPGovernanceCockpit-HTML5
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true